syntax = "proto3";

package user;

service UserService {
    rpc GetUser (UserId) returns (User) {}
}

message User {
    string _id = 1;
    string name = 2;
    string email = 3;
}

message UserId {
    string id = 1;
}
